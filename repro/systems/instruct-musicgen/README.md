# Instruct-MusicGen

- Official repo: `ldzhangyx/instruct-MusicGen`
- Locked revision: `037c2da2458dc07a745ff7ddbef15a3a28082b4c`
- Checked: 2026-09-17

## Documented setup
`pip install -r requirements.txt`, or use `environment.yaml`.

## Minimal entry point
`python src/inference.py`

## Evaluation
`python src/eval.py`, followed by the scripts under `evaluation/` for the released evaluation workflow.

The checked README explicitly states that the public retrained checkpoint is not the Sony official checkpoint.

## Runtime status
`NOT_TESTED`
